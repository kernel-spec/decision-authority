# Decision Authority — Environment variables reference
# Copy this file to .env and fill in your values.
# NEVER commit real secrets. Worker secrets must be set via `wrangler secret put`.

# ── decision-api (wrangler.toml [vars]) ──────────────────────────────────────
# Runtime environment label (dev | stage | prod)
ENV=dev

# Policy version to stamp on each decision run
POLICY_VERSION=v1

# Engine version to stamp on each decision run
ENGINE_VERSION=v0.1.0

# Enforce that a trigger spec is present (true | false)
REQUIRE_TRIGGERS=true

# Enforce that gating rules are present (true | false)
REQUIRE_GATING=true

# ── admin-api secrets (set via `wrangler secret put ADMIN_KEY --env <env>`) ──
# Admin API bearer key — do NOT paste into AI assistants or commit here
# ADMIN_KEY=<redacted>

# ── Cloudflare (GitHub Actions secrets) ──────────────────────────────────────
# Used by wrangler in CI/CD deploy workflows
# CLOUDFLARE_API_TOKEN=<redacted>
# CLOUDFLARE_ACCOUNT_ID=<redacted>
